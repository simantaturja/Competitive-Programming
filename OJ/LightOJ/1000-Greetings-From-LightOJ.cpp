#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    int tc, kase = 1;
    scanf("%d", &tc);
    while (tc--) {
        int a, b;
        scanf("%d %d", &a, &b);
        printf("Case %d: %d\n", kase++, a + b);
    }
    return 0;
}